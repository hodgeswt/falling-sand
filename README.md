# Falling Sand

A simple falling sand simulation implemented in TypeScript and HTML5 Canvas.

Fun project to experiment with TS/HTML.

# To Run
1. Clone the repository
2. Install dependencies with `npm install`
3. Build with `npm run build`
4. Open the `build/index.html` in a browser